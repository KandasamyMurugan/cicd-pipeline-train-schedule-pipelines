pipeline {
   agent any
   stages {
     stage ('Build') {
     steps {
       echo "Running the Build"
       sh './gradlew build' --no-daemon
       archiveArtifacts artifacts: 'dist/Trainshedule.zip'
       
     }
     }
}
   
}
